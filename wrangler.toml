name = "qrcode-generator"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# 开发时可使用本地模拟 R2
# 部署前需要在 Cloudflare Dashboard 创建 R2 存储桶

[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "qrcode-images"

# 如果需要自定义域名，取消下面的注释
# [routes]
# patterns = ["your-domain.com/*"]

